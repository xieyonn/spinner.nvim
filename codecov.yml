coverage:
  status:
    project:
      default:
        target: "auto"
        threshold: "80"
        strict: "no"
        patch: "yes"
  patch:
    default:
      threshold: "0"
      strict: "no"
notify:
  require_ci_to_pass: yes
