coverage:
  status:
    project:
      default:
        target: "90"
        threshold: "0"
        strict: "no"
        patch: "yes"
  patch:
    default:
      target: "90"
      threshold: "0"
      strict: "no"
notify:
  require_ci_to_pass: yes
